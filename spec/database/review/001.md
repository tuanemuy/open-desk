# DB設計レビュー #1

## 指摘事項

1. **spaces.default_thread_id**: NOT NULL + FK 制約が欠落
2. **organizations.parent_organization_id**: ON DELETE CASCADE → RESTRICT に変更すべき（子組織がある場合の削除防止）
3. **thread_comments**: text/files のいずれか必須の CHECK 制約が欠落
4. **notification_filter_sender_conditions**: (filter_id, sender_type, sender_id) の UNIQUE 制約が欠落
5. **record_comments/thread_comments**: like_count のデノーマライゼーションカラム追加を推奨
