# 動作確認計画 — Issue #9: リファクタ: system-mail/index.tsx のインライン loader を loader.server.ts に分離する

**Issue:** #9
**作成日:** 2026-04-12

---

## 確認環境

このIssueの変更を確認するために必要な手順のみ記載（プロジェクト全体のセットアップは省略）。

### デプロイ・起動手順
```bash
pnpm build      # ビルドエラーがないか確認
pnpm dev        # 開発サーバーを起動
```

## 確認項目

### 1. システムメール画面の正常表示

- **目的:** loader 分離後もデータが正しく読み込まれ、画面が表示されることを確認する
- **手順:**
  1. `pnpm dev` で開発サーバーを起動
  2. ブラウザで管理画面にログイン
  3. `/admin/system-mail` にアクセス
- **期待結果:** システムメール画面が正常に表示され、メールアドレスやサーバー種別が表示される
- **確認ポイント:** fromAddress と serverType が正しく表示されること

### 2. メール設定の更新

- **目的:** loader だけでなく action（フォーム送信）も影響を受けていないことを確認する
- **手順:**
  1. `/admin/system-mail` でサーバー種別のラジオボタンを変更
  2. 「保存」ボタンをクリック
- **期待結果:** 「メール設定を保存しました」のトースト通知が表示される
- **確認ポイント:** ページリロード後も変更が保持されること

## エッジケース・異常系

### 1. 未認証時のアクセス

- **目的:** requireAuth によるリダイレクトが正しく動作することを確認する
- **手順:**
  1. ログアウト状態で `/admin/system-mail` にアクセス
- **期待結果:** ログインページにリダイレクトされる

## 既存機能への影響確認

- 他の admin 配下のルート（`/admin/directory`, `/admin/group` 等）に影響がないこと（変更していないため問題ないはずだが念のため確認）

## 確認チェックリスト

- [ ] `pnpm typecheck` が通る
- [ ] `pnpm build` が通る
- [ ] `/admin/system-mail` が正常に表示される
- [ ] メール設定の保存が動作する
- [ ] 未認証時にリダイレクトされる
