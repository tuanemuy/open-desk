# 動作確認計画 — Issue #11: fix: action内のtry-catchがrequireAuthのredirectを飲み込む

**Issue:** #11
**作成日:** 2026-04-12

---

## 確認環境

このIssueの変更を確認するために必要な手順のみ記載（プロジェクト全体のセットアップは省略）。

### デプロイ・起動手順
```bash
pnpm build      # ビルドエラーがないか確認
pnpm dev        # 開発サーバーを起動
```

## 確認項目

### 1. セッション切れ時のリダイレクト（apps/store）

- **目的:** アプリ作成時にセッションが切れていた場合、ログインページにリダイレクトされることを確認する
- **手順:**
  1. `pnpm dev` で開発サーバーを起動
  2. ブラウザでログインし、アプリ作成画面を開く
  3. ブラウザの開発者ツールでセッションCookieを削除する
  4. アプリ作成フォームを送信する
- **期待結果:** ログインページ（`/login`）にリダイレクトされる
- **確認ポイント:** フォームに "Authentication required" エラーが表示されるのではなく、リダイレクトが発生すること

### 2. セッション切れ時のリダイレクト（records/new）

- **目的:** レコード作成時にセッションが切れていた場合、ログインページにリダイレクトされることを確認する
- **手順:**
  1. ログインし、レコード作成画面を開く
  2. セッションCookieを削除する
  3. レコード作成フォームを送信する
- **期待結果:** ログインページにリダイレクトされる

### 3. セッション切れ時のリダイレクト（records/record）

- **目的:** コメント投稿時にセッションが切れていた場合、ログインページにリダイレクトされることを確認する
- **手順:**
  1. ログインし、レコード詳細画面を開く
  2. セッションCookieを削除する
  3. コメントを入力して送信する
- **期待結果:** ログインページにリダイレクトされる

### 4. セッション切れ時のリダイレクト（spaces/new）

- **目的:** スペース作成時にセッションが切れていた場合、ログインページにリダイレクトされることを確認する
- **手順:**
  1. ログインし、スペース作成画面を開く
  2. セッションCookieを削除する
  3. スペース作成フォームを送信する
- **期待結果:** ログインページにリダイレクトされる

## エッジケース・異常系

### 1. 認証済みユーザーの正常動作

- **目的:** 修正後も認証済みユーザーの操作が正常に動作することを確認する
- **手順:**
  1. 正常にログインした状態で各フォーム操作を実行する
- **期待結果:** 従来通り正常に動作する（リグレッションがないこと）

## 既存機能への影響確認

- 他の action.server.ts（既に正しいパターンを使用）に変更を加えていないため、影響なし

## 確認チェックリスト

- [ ] `pnpm typecheck` が通る
- [ ] `pnpm lint:fix` が通る
- [ ] `pnpm build` が通る
- [ ] セッション切れ時にリダイレクトされる（4画面すべて）
- [ ] 認証済みユーザーの操作が正常に動作する
